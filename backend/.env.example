# Database configuration path (defaults to pipeline_monitor.db)
PIPELINE_MONITOR_DB=pipeline_monitor.db

# Databricks Credentials (to run in Live mode)
# DATABRICKS_HOST=https://adb-xxxx.azuredatabricks.net
# DATABRICKS_TOKEN=dapi-xxxx
